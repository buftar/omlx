---
phase: 07-benchmark-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_compression_benchmark.py
  - omlx/compression/benchmark.py
  - omlx/compression/evaluators.py
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03, VAL-04, VAL-05, VAL-06, VAL-07, VAL-08]

must_haves:
  truths:
    - "pytest tests/test_compression_benchmark.py -m 'not slow' -x exits with code 1 (RED — all tests fail with NotImplementedError)"
    - "All 8 VAL requirement stubs exist as distinct test classes with the correct class names"
    - "BenchmarkRunner, run_gsm8k, run_mmlu, run_litm are importable (no ImportError)"
  artifacts:
    - path: "tests/test_compression_benchmark.py"
      provides: "Full test scaffold covering VAL-01 through VAL-08"
      contains: "class TestBenchmarkReport, class TestReproducibility, class TestSwaDetection, class TestSlowQwen, class TestSlowLlama, class TestSlowGemma, class TestSlowDeepSeek"
    - path: "omlx/compression/benchmark.py"
      provides: "BenchmarkRunner stub raising NotImplementedError"
      exports: ["BenchmarkRunner", "benchmark_compression_command"]
    - path: "omlx/compression/evaluators.py"
      provides: "Evaluator stubs raising NotImplementedError"
      exports: ["run_gsm8k", "run_mmlu", "run_litm", "cosine_sim_kv", "measure_decompression_latency"]
  key_links:
    - from: "tests/test_compression_benchmark.py"
      to: "omlx/compression/benchmark.py"
      via: "import BenchmarkRunner"
      pattern: "from omlx.compression.benchmark import BenchmarkRunner"
    - from: "tests/test_compression_benchmark.py"
      to: "omlx/compression/evaluators.py"
      via: "import evaluator functions"
      pattern: "from omlx.compression.evaluators import"
---

<objective>
Create the Wave 0 RED-state test scaffold for Phase 7: test file with all VAL stubs, benchmark.py stub, and evaluators.py stub. All fast tests fail with NotImplementedError.

Purpose: Establish the RED baseline so Wave 1 implementation turns tests GREEN incrementally. All 8 VAL requirement IDs have test coverage from day one.
Output: 3 new files; pytest non-slow run exits code 1 (RED).
</objective>

<execution_context>
@/Users/tonysina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonysina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-benchmark-suite/07-RESEARCH.md
@.planning/phases/07-benchmark-suite/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create benchmark.py and evaluators.py stubs</name>
  <files>omlx/compression/benchmark.py, omlx/compression/evaluators.py</files>
  <action>
Create `omlx/compression/benchmark.py` with:
- SPDX license header: `# SPDX-License-Identifier: Apache-2.0`
- `BenchmarkRunner` class with constructor `__init__(self, model_path, bundle_path=None, am_ratio=4.0, seed=42, n_samples=200)` that raises `NotImplementedError("BenchmarkRunner not yet implemented")`
- `run_benchmark(self, tasks=None)` method that raises `NotImplementedError`
- `benchmark_compression_command(args)` top-level function that raises `NotImplementedError`

Create `omlx/compression/evaluators.py` with:
- SPDX license header
- The following stub functions, each raising `NotImplementedError`:
  - `run_gsm8k(model, tokenizer, pipeline, n_samples=200, seed=42)` — returns dict with vanilla/compressed accuracy
  - `run_mmlu(model, tokenizer, pipeline, n_samples=200, seed=42)` — returns dict with vanilla/compressed accuracy
  - `run_litm(model, tokenizer, pipeline, n_problems=100, seed=42)` — returns dict with vanilla/compressed recall
  - `cosine_sim_kv(layers_a, layers_b)` — returns float
  - `measure_decompression_latency(pipeline, blob, n_layers, n_warmup=1)` — returns float (ms/layer)
  - `get_compressible_layer_indices(prompt_cache)` — returns list of int (non-SWA layer indices)
  - `detect_swa_layers(model_path, num_hidden_layers)` — returns set of int

Do NOT add any implementation logic — stubs only. Import guards are allowed at module level for graceful fallback (wrap `mlx_lm`, `datasets` imports in try/except ImportError as in prior phases).
  </action>
  <verify>
    <automated>cd /Users/tonysina/projects/omlx && python -c "from omlx.compression.benchmark import BenchmarkRunner, benchmark_compression_command; from omlx.compression.evaluators import run_gsm8k, run_mmlu, run_litm, cosine_sim_kv, measure_decompression_latency, get_compressible_layer_indices, detect_swa_layers; print('imports OK')"</automated>
  </verify>
  <done>All 7 stub functions and BenchmarkRunner importable without error; each raises NotImplementedError when called.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_compression_benchmark.py scaffold (RED state)</name>
  <files>tests/test_compression_benchmark.py</files>
  <action>
Create `tests/test_compression_benchmark.py` with SPDX header. Structure:

```python
# SPDX-License-Identifier: Apache-2.0
"""
Benchmark suite tests — Phase 7: VAL-01 through VAL-08.
Wave 0 scaffold: all tests RED (NotImplementedError from stubs).
"""
import pytest
import numpy as np
from omlx.compression.benchmark import BenchmarkRunner
from omlx.compression.evaluators import (
    cosine_sim_kv, get_compressible_layer_indices, detect_swa_layers,
)
```

Test classes to create (every class must have at least one test method):

**TestBenchmarkReport** (VAL-01): `test_report_has_required_fields` — instantiates `BenchmarkRunner("fake/model")`, calls `run_benchmark(tasks=["cosine_sim"])`, expects `NotImplementedError` for now; also `test_report_schema_version` — same pattern.

**TestReproducibility** (VAL-08): `test_same_seed_produces_same_report` — calls `BenchmarkRunner("fake/model", seed=42).run_benchmark()` twice, expects `NotImplementedError`.

**TestSwaDetection** (VAL-06, unit): Two tests that do NOT require a live model:
- `test_non_gemma3_returns_empty_set` — calls `detect_swa_layers("/fake/path/non-gemma", 28)` but with a fixture that patches the config.json read. Since stub raises NotImplementedError, use `pytest.raises(NotImplementedError)`.
- `test_get_compressible_layer_indices_filters_rotating` — creates a mock prompt_cache list of fake objects (using a simple class or MagicMock with `isinstance` behavior), expects NotImplementedError from stub.

**TestSlowQwen** (VAL-02, VAL-03, VAL-04): Mark entire class `@pytest.mark.slow`. Three test methods:
- `test_am_cosine_sim` — calls BenchmarkRunner with real model path, expects NotImplementedError
- `test_task_accuracy` — calls BenchmarkRunner, expects NotImplementedError
- `test_full_pipeline_runs` — calls BenchmarkRunner, expects NotImplementedError

**TestSlowLlama** (VAL-05): Mark `@pytest.mark.slow`. One test: `test_llama_pipeline_runs` — expects NotImplementedError.

**TestSlowGemma** (VAL-06, slow): Mark `@pytest.mark.slow`. One test: `test_gemma_swa_layers_skipped` — expects NotImplementedError.

**TestSlowDeepSeek** (VAL-07): Mark `@pytest.mark.slow`. One test: `test_deepseek_pipeline_runs` — expects NotImplementedError.

For the non-slow test classes, the pattern is: call the stub function, assert it raises `NotImplementedError`. This is the valid Wave 0 RED pattern used in prior phases. Do NOT write assertions that would pass — the point is that fast tests run but stub code raises errors.

IMPORTANT: For `TestSwaDetection`, the `test_get_compressible_layer_indices_filters_rotating` test should directly test the function signature without needing a live model — construct a fake list with plain objects and assert NotImplementedError (since stub raises it regardless of input).
  </action>
  <verify>
    <automated>cd /Users/tonysina/projects/omlx && pytest tests/test_compression_benchmark.py -m "not slow" -x --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>pytest non-slow run exits with code 1 (tests fail with NotImplementedError or collection succeeds but assertions fail — NOT code 2 which would mean ImportError). All 7+ test classes are collected. Output includes "FAILED" lines, not "ERROR" lines.</done>
</task>

</tasks>

<verification>
pytest tests/test_compression_benchmark.py -m "not slow" --collect-only 2>&1 | grep "test session starts\|collected\|TestBenchmark\|TestReproducibility\|TestSwa"
</verification>

<success_criteria>
- 3 new files exist with SPDX headers
- All stubs importable (no ImportError)
- pytest collects at least 7 test classes
- pytest -m "not slow" exits with code 1 (RED), not code 2 (collection error)
- Slow tests collected but not run in fast mode
</success_criteria>

<output>
After completion, create `.planning/phases/07-benchmark-suite/07-01-SUMMARY.md`
</output>
