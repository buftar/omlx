---
phase: 02-am-compaction
plan: 03
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - omlx/compression/am.py
autonomous: true
requirements:
  - AM-05
  - AM-06
  - AM-07

must_haves:
  truths:
    - "Non-uniform head budgets are computed using entropy proportions stored at AMCompactor.__init__ time"
    - "Per-head budgets are proportional to entropy; higher-entropy heads receive more tokens"
    - "No per-head budget falls below n_sink_tokens"
    - "Per-head budgets sum to n_heads * floor(T/ratio) with rounding corrected via highest-entropy head adjustment"
    - "compact() uses entropy proportions from __init__; it does not recompute them per call"
    - "generate_reference_queries returns shape [1, n_heads, n_queries, head_dim] for both sample and random methods"
    - "All 10 AM test classes pass: pytest tests/test_am.py -v reports zero failures"
  artifacts:
    - path: "omlx/compression/am.py"
      provides: "Complete AMCompactor with non-uniform head budgets and generate_reference_queries helper"
      exports: ["AMCompactedCache", "AMCompactor", "generate_reference_queries"]
      min_lines: 200
  key_links:
    - from: "AMCompactor.__init__"
      to: "self._head_entropy"
      via: "stored at init time; entropy proportions derived from it in _compute_head_budgets"
      pattern: "_head_entropy"
    - from: "AMCompactor.compact"
      to: "AMCompactor._compute_head_budgets"
      via: "called once per compact() invocation using stored entropy proportions"
      pattern: "_compute_head_budgets"
---

<objective>
Complete AMCompactor with entropy-proportional non-uniform head budgets and the generate_reference_queries helper function.

Purpose: AM-05 and AM-06 require entropy curves stored at init and used at compact() time. AM-07 requires the generate_reference_queries convenience helper. After this plan, all 10 test classes in test_am.py pass and Phase 2 is feature-complete.
Output: Final am.py with _compute_head_budgets, updated compact() using per-head budgets, and generate_reference_queries at module level.
</objective>

<execution_context>
@/Users/tonysina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonysina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-am-compaction/02-CONTEXT.md
@.planning/phases/02-am-compaction/02-RESEARCH.md
@.planning/phases/02-am-compaction/02-02-SUMMARY.md

<interfaces>
<!-- Current compact() uniform budget logic from Plan 02 (to be upgraded) -->

Plan 02 compact() budget line (to replace):
  budget_per_head = max(self.n_sink_tokens, int(seq_len / ratio))
  # Same budget used for every head

Target after this plan:
  head_budgets = self._compute_head_budgets(seq_len, ratio, n_heads)  # list[int], length n_heads
  # In inner head loop: self._compact_head(k_h, v_h, q_h, head_budgets[h])

Non-uniform budget formula (from 02-RESEARCH.md):
  ent = np.array(self._head_entropy, dtype=np.float64)   # shape [n_heads]
  total_budget = n_heads * max(n_sink_tokens, int(seq_len / ratio))
  proportions = ent / ent.sum()
  budgets = np.maximum(n_sink_tokens, np.round(proportions * total_budget).astype(int))
  diff = total_budget - int(budgets.sum())   # rounding correction
  if diff != 0:
      budgets[np.argmax(ent)] += diff
  return budgets.tolist()

Spike example values (Qwen 2.5 7B layer 0, 4 heads):
  head_entropy = [1.68, 0.34, 2.47, 1.12]
  At T=501, ratio=4: head 2 (entropy 2.47) gets the most tokens, head 1 (0.34) gets fewest

generate_reference_queries (locked CONTEXT.md API):
  Signature: generate_reference_queries(keys, n_queries=64, method="sample") -> mx.array
  keys shape: [1, n_heads, seq_len, head_dim]
  Returns:   [1, n_heads, n_queries, head_dim]

  "sample" path:
    seq_len = keys.shape[2]
    indices = mx.random.randint(0, seq_len, shape=(n_queries,))
    queries = keys[:, :, indices, :]
    # force materialization, then return

  "random" path:
    key_std = mx.std(keys).item()
    queries = mx.random.normal(shape=(keys.shape[0], keys.shape[1], n_queries, keys.shape[3])) * key_std
    # force materialization, then return
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Non-uniform head budget computation</name>
  <files>omlx/compression/am.py</files>
  <behavior>
    - _compute_head_budgets(seq_len, ratio, n_heads) returns a list[int] of length n_heads
    - Each budget in the list is >= n_sink_tokens (enforced via np.maximum)
    - With head_entropy provided, budgets are proportional to entropy values
    - Budgets sum to n_heads * floor(seq_len / ratio); rounding error is corrected via highest-entropy head
    - With head_entropy=None, returns uniform list (same value for all heads)
    - compact() calls _compute_head_budgets at the start and passes head_budgets[h] to _compact_head(...)
    - TestHeadBudgets::test_uniform_budgets_correct passes
    - TestHeadBudgets::test_entropy_budgets_proportional passes
    - TestHeadBudgets::test_entropy_budgets_min_sinks passes
    - TestBudgetReuse::test_budgets_computed_at_init passes
  </behavior>
  <action>
Add _compute_head_budgets as a private method on AMCompactor with this signature:
  def _compute_head_budgets(self, seq_len: int, ratio: float, n_heads: int) -> list[int]

Implementation steps inside the method:
1. uniform_budget = max(self.n_sink_tokens, int(seq_len / ratio))
2. If self._head_entropy is None: return [uniform_budget] * n_heads
3. ent = np.array(self._head_entropy, dtype=np.float64)
4. If ent.sum() == 0: return [uniform_budget] * n_heads
5. proportions = ent / ent.sum()
6. total_budget = n_heads * uniform_budget
7. budgets = np.maximum(self.n_sink_tokens, np.round(proportions * total_budget).astype(int))
8. diff = total_budget - int(budgets.sum())
9. if diff != 0: budgets[np.argmax(ent)] += diff
10. return budgets.tolist()

Update compact() method body: replace the line
  budget_per_head = max(self.n_sink_tokens, int(seq_len / ratio))
with:
  head_budgets = self._compute_head_budgets(seq_len, ratio, n_heads)

In the inner head loop, replace the _compact_head call argument from budget_per_head to head_budgets[h].

__init__ already stores self._head_entropy from Plan 02. No __init__ change needed.
  </action>
  <verify>
    <automated>pytest tests/test_am.py::TestHeadBudgets tests/test_am.py::TestBudgetReuse -x -v</automated>
  </verify>
  <done>TestHeadBudgets and TestBudgetReuse all pass. Previously passing tests remain green: run pytest tests/test_am.py -x -v to confirm no regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generate_reference_queries module-level helper</name>
  <files>omlx/compression/am.py</files>
  <behavior>
    - generate_reference_queries is a module-level function (not a method) placed after AMCompactor class
    - "sample" method returns shape [1, n_heads, n_queries, head_dim] by sampling from key sequence positions
    - "random" method returns shape [1, n_heads, n_queries, head_dim] with Gaussian noise scaled to key std
    - Both methods call mx.eval on the result before returning (MLX graph materialization)
    - Docstring notes that queries=None in compact() is the lower-quality uniform fallback; callers should use this helper for production-quality compaction
    - TestGenerateReferenceQueries::test_sample_method_shape passes
    - TestGenerateReferenceQueries::test_random_method_shape passes
    - Full suite: pytest tests/test_am.py -v reports zero failures across all 10 test classes
  </behavior>
  <action>
Add generate_reference_queries as a module-level function after the AMCompactor class.

Exact function body:

def generate_reference_queries(
    keys: mx.array,
    n_queries: int = 64,
    method: str = "sample",
) -> mx.array:
    """Generate reference queries for use with AMCompactor.compact().

    Args:
        keys: KV cache keys tensor, shape [1, n_heads, seq_len, head_dim].
        n_queries: Number of reference queries to generate.
        method: Strategy for query generation.
            "sample" -- randomly sample n_queries positions from existing keys
                        (production default).
            "random" -- Gaussian noise scaled to key std (useful for ablation).
            Note: passing the result as queries= to compact() is the production
            path. queries=None in compact() is a testing-only uniform fallback
            with no cosine similarity guarantee.

    Returns:
        mx.array of shape [1, n_heads, n_queries, head_dim].
    """
    seq_len = keys.shape[2]
    if method == "sample":
        indices = mx.random.randint(0, seq_len, shape=(n_queries,))
        queries = keys[:, :, indices, :]
    else:  # "random"
        key_std = mx.std(keys).item()
        queries = mx.random.normal(
            shape=(keys.shape[0], keys.shape[1], n_queries, keys.shape[3])
        ) * key_std
    mx.eval(queries)
    return queries

After writing the function, confirm the import smoke test passes:
  python -c "from omlx.compression.am import AMCompactor, AMCompactedCache, generate_reference_queries; print('ok')"

Then run the full test suite to confirm all 10 test classes are green.
  </action>
  <verify>
    <automated>pytest tests/test_am.py -v</automated>
  </verify>
  <done>All 10 test classes in tests/test_am.py pass with zero failures. pytest tests/test_linalg_utils.py::test_no_bare_linalg_calls -x also passes (lint gate clean). generate_reference_queries is importable from omlx.compression.am.</done>
</task>

</tasks>

<verification>
Full Phase 2 gate after both tasks complete:

  pytest tests/test_am.py -v

Expected: all 10 test classes pass (TestHighestAttnKeysSelection, TestUniformFallback,
TestNNLSBetaFitting, TestOLSValueFitting, TestCompactedCacheShape, TestHeadBudgets,
TestBudgetReuse, TestGenerateReferenceQueries, TestBetaBoxConstraint, TestCompactIntegration).

Lint gate:
  pytest tests/test_linalg_utils.py::test_no_bare_linalg_calls -x

Import smoke test:
  python -c "from omlx.compression.am import AMCompactor, AMCompactedCache, generate_reference_queries; print('ok')"
</verification>

<success_criteria>
All AM requirements complete: AM-01 through AM-08. All 10 test classes pass. generate_reference_queries works for both "sample" and "random" methods. Phase 2 is ready for /gsd:verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-am-compaction/02-03-SUMMARY.md`
</output>
