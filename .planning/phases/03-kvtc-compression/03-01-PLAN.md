---
phase: 03-kvtc-compression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - omlx/compression/kvtc.py
  - tests/test_kvtc.py
autonomous: true
requirements: [KVTC-01, KVTC-02, KVTC-03, KVTC-04, KVTC-05, KVTC-06, KVTC-07]

must_haves:
  truths:
    - "pytest tests/test_kvtc.py -m not-slow exits with code 1 (RED — all implementation tests fail with NotImplementedError)"
    - "All 8 test classes are present in tests/test_kvtc.py covering KVTC-01 through KVTC-07 plus integration"
    - "import zstandard succeeds in the project venv (no ModuleNotFoundError at test collection time)"
    - "pytest.mark.slow is registered — no PytestUnknownMarkWarning on any slow-marked test"
    - "KVTCCompressor can be imported from omlx.compression.kvtc without error"
  artifacts:
    - path: "tests/test_kvtc.py"
      provides: "Complete RED test scaffold for all KVTC requirements"
      min_lines: 200
    - path: "omlx/compression/kvtc.py"
      provides: "KVTCCompressor stub with correct class signature and NotImplementedError bodies"
      contains: "class KVTCCompressor"
    - path: "pyproject.toml"
      provides: "zstandard dependency + slow marker registration"
      contains: "zstandard"
  key_links:
    - from: "tests/test_kvtc.py"
      to: "omlx/compression/kvtc.py"
      via: "from omlx.compression.kvtc import KVTCCompressor"
      pattern: "from omlx.compression.kvtc import KVTCCompressor"
---

<objective>
Wave 0: Create the test scaffold and dependency foundation that puts Phase 3 in RED state -- all tests imported, all test classes present, all failing with NotImplementedError (not ImportError), zstandard installed, pytest.mark.slow registered.

Purpose: Every subsequent plan in Phase 3 implements against these test classes. RED state confirms the import path is clean and the implementation stub correctly surfaces failures.
Output: tests/test_kvtc.py (8 test classes), omlx/compression/kvtc.py (stub), pyproject.toml updates.
</objective>

<execution_context>
@/Users/tonysina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tonysina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-kvtc-compression/03-CONTEXT.md
@.planning/phases/03-kvtc-compression/03-RESEARCH.md

<interfaces>
<!-- Established patterns from omlx/compression/am.py (mirror these in kvtc.py) -->
<!--
Module-level materialization alias (established in am.py line 29-30):
  _mx_materialize = mx.eval
  NOTE: This is MLX lazy graph materialization (forces tensor computation).
  It is NOT Python built-in eval(). It does not execute strings or arbitrary code.
  The alias name documents intent and avoids security scanner confusion.

Optional-bundle constructor pattern (am.py):
  class AMCompactor:
      def __init__(self, head_entropy=None, n_sink_tokens: int = 4):

KV cache shape contract: [1, n_kv_heads, seq_len, head_dim]

linalg_utils exports (for on-the-fly SVD fallback):
  from omlx.compression.linalg_utils import svd_f32
-->

KVTCCompressor API (locked in 03-CONTEXT.md):
  class KVTCCompressor:
      def __init__(self, pca_bundle=None, n_sink_tokens: int = 4, sliding_window: int = 128, bits_per_token: float = 4.0)
      def compress(self, kv_cache: list[tuple[mx.array, mx.array]]) -> bytes
      def decompress(self, blob: bytes) -> list[tuple[mx.array, mx.array]]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add zstandard dependency and register pytest.mark.slow</name>
  <files>pyproject.toml</files>
  <action>
Edit pyproject.toml with two additions:

1. Add zstandard to [project] dependencies (production dependency, not dev-only):
   Add "zstandard>=0.21.0" to the dependencies list under [project].

2. Add markers to [tool.pytest.ini_options]:
   markers = [
       "slow: marks tests as slow -- require a real model download (deselect with '-m not slow')",
   ]

After editing pyproject.toml, reinstall so zstandard is in the venv:
  pip install -e ".[dev]"

Verify:
  python -c "import zstandard; print('zstandard ok')"
  </action>
  <verify>
    <automated>cd /Users/tonysina/projects/omlx && python -c "import zstandard; print('ok')"</automated>
  </verify>
  <done>zstandard imports without error; pyproject.toml [project] dependencies contains zstandard; [tool.pytest.ini_options] has markers list with slow marker</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create kvtc.py stub and tests/test_kvtc.py scaffold (RED state)</name>
  <files>omlx/compression/kvtc.py, tests/test_kvtc.py</files>
  <behavior>
    - Import: from omlx.compression.kvtc import KVTCCompressor succeeds (no ImportError at collection time)
    - Constructor test passes: KVTCCompressor(pca_bundle=None) constructs, n_sink_tokens==4, sliding_window==128
    - All implementation tests fail with NotImplementedError (RED state)
    - 8 named test classes present: TestPCAProjection, TestDPAllocation, TestCompressDecompress, TestRoundTrip, TestSinkTokenExemption, TestWindowTokenExemption, TestGQAShapeContract, TestDecompressionLatency
    - pytest -m "not slow" exits 1 (not 0)
    - No PytestUnknownMarkWarning in output
  </behavior>
  <action>
Step 1: Create omlx/compression/kvtc.py

File must begin with:
  # SPDX-License-Identifier: Apache-2.0

Module docstring explains: PCA-based KV cache compressor. Production path uses
pca_bundle from Phase 4. Testing path uses on-the-fly PCA (documented as lower
quality). RoPE stripping is the caller's responsibility (not done inside kvtc.py).

IMPORTANT: The file must define a module-level alias for mx.eval with a comment
explaining it is MLX lazy graph materialization (forces tensor computation), NOT
Python's built-in eval(). The alias name should be _mx_materialize. This alias
is used before any numpy bridge conversions in later implementation tasks.
The comment in the source file must clearly state this is NOT eval() of strings.

Class: KVTCCompressor
  - Constructor: __init__(self, pca_bundle=None, n_sink_tokens: int = 4,
                          sliding_window: int = 128, bits_per_token: float = 4.0)
  - Method: compress(self, kv_cache: list[tuple[mx.array, mx.array]]) -> bytes
    -- docstring: keys must have RoPE stripped by caller; returns self-describing blob
    -- body: raise NotImplementedError("KVTCCompressor.compress() not yet implemented")
  - Method: decompress(self, blob: bytes) -> list[tuple[mx.array, mx.array]]
    -- docstring: blob is self-describing, no bundle required at decompress time
    -- body: raise NotImplementedError("KVTCCompressor.decompress() not yet implemented")

Step 2: Create tests/test_kvtc.py

File must begin with:
  # SPDX-License-Identifier: Apache-2.0

Module docstring: explains Wave 0 RED state, how to run fast vs all tests.

Imports: time, mlx.core as mx, numpy as np, pytest, KVTCCompressor

Fixtures (module-level):
  small_kv_cache: 2-layer, 4-head GQA, seq_len=300, head_dim=128, float16
                  (mx.random.seed(42), shape [1, 4, 300, 128] per tensor)
                  seq_len=300 ensures body is non-empty: 300 > 4+128=132
  gqa_kv_cache:   28-layer, 4 KV heads, seq_len=300, head_dim=128, float16
                  (mx.random.seed(7), Qwen 2.5 7B config)

Test classes (8 total):

class TestPCAProjection (KVTC-01):
  - test_compressor_constructs_without_bundle: construct KVTCCompressor(None), check defaults -- must PASS
  - test_compress_raises_not_implemented: compress() raises NotImplementedError
  - test_onthefly_pca_produces_bytes: compress() returns bytes (will pass after Wave 1)
  - test_pca_projection_preserves_shape: decompressed shape matches input (after Wave 2)

class TestDPAllocation (KVTC-02):
  - test_compress_raises_not_implemented: compress() raises NotImplementedError
  - test_dp_all_bits_at_least_one: all bit alloc >= 1 (verified via successful round-trip)
  - test_dp_within_budget: blob smaller than raw float16 size * 0.9

class TestCompressDecompress (KVTC-03):
  - test_compress_returns_bytes: compress() raises NotImplementedError (RED)
  - test_blob_has_kvtc_magic: blob starts with b"KVTC"
  - test_blob_is_deterministic: same input -> identical blob

class TestRoundTrip (KVTC-04):
  - test_round_trip_raises_not_implemented: compress() raises NotImplementedError (RED)
  - test_round_trip_cosine_similarity: body tokens cosine sim >= 0.97
  - test_real_qwen_round_trip: @pytest.mark.slow, pytest.skip() stub

class TestSinkTokenExemption (KVTC-05):
  - test_compress_raises_not_implemented: compress() raises NotImplementedError (RED)
  - test_sink_tokens_preserved_exactly: first n_sink_tokens bit-identical after round-trip
  - test_sink_exemption_is_load_bearing: stub confirming ablation test (Wave 2)

class TestWindowTokenExemption (KVTC-06):
  - test_compress_raises_not_implemented: compress() raises NotImplementedError (RED)
  - test_window_tokens_preserved_exactly: last sliding_window tokens bit-identical after round-trip
  - test_short_sequence_guard: seq_len=100 < 132 does not crash; stores all verbatim

class TestGQAShapeContract (KVTC-07):
  - test_compress_raises_not_implemented: compress() raises NotImplementedError (RED) on gqa_kv_cache
  - test_gqa_shape_preserved: decompressed has [1, 4, seq_len, 128] shape
  - test_kv_head_count_independent_of_query_heads: 8-head MHA cache also works

class TestDecompressionLatency:
  - test_decompression_raises_not_implemented: compress() raises NotImplementedError (RED)
  - test_decompression_latency_under_10ms_per_layer: 8K context (seq_len=8192, 28 layers),
    time.perf_counter() around decompress(), assert per_layer_ms < 10.0

Step 3: Confirm RED state
  cd /Users/tonysina/projects/omlx
  uv run python -m pytest tests/test_kvtc.py -m "not slow" -q 2>&1 | tail -10
  Expect: exit code 1, multiple failures with NotImplementedError
  </action>
  <verify>
    <automated>cd /Users/tonysina/projects/omlx && uv run python -m pytest tests/test_kvtc.py -m "not slow" --tb=no -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest -m "not slow" exits 1; from omlx.compression.kvtc import KVTCCompressor succeeds; 8 test classes present; no PytestUnknownMarkWarning; at least test_compressor_constructs_without_bundle passes</done>
</task>

</tasks>

<verification>
1. uv run python -m pytest tests/test_kvtc.py -m "not slow" -q exits 1 (RED state)
2. python -c "import zstandard" succeeds (no ModuleNotFoundError)
3. python -c "from omlx.compression.kvtc import KVTCCompressor" succeeds
4. No PytestUnknownMarkWarning in output
5. grep "class KVTCCompressor" omlx/compression/kvtc.py returns a match
6. grep "zstandard" pyproject.toml returns a match in [project] dependencies
</verification>

<success_criteria>
- 8 test classes in tests/test_kvtc.py (one per requirement KVTC-01..07 plus latency)
- pytest -m "not slow" exits 1 (RED -- implementation tests failing, not collection errors)
- zstandard importable in project venv
- pyproject.toml: zstandard in [project] dependencies, markers list in [tool.pytest.ini_options]
- KVTCCompressor importable without error
</success_criteria>

<output>
After completion, create .planning/phases/03-kvtc-compression/03-01-SUMMARY.md
</output>
